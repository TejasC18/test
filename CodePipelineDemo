pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building'
            }
        }
        stage('run') {
            steps {
                echo 'Running'
                bat 'node funfor.js'
            }
        }
    }
}
